name: MegaLinter

on:
  pull_request:
    branches:
      - master

jobs:
  megalinter:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write
      checks: write

    steps:
      # Step 1: Checkout the full git history safely
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Step 2: Updated TruffleHog with explicit fail and base control flags
      - name: Scan for Leaked Secrets (TruffleHog)
        uses: trufflesecurity/trufflehog@main
        with:
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.sha }}
          extra_args: --fail --debug # Force an exit code failure if ANY secret pattern is found

      # Step 3: Initialize Runtime Environment
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # Step 4: Provision Local Node Packages
      - name: Install Dependencies
        run: |
          npm install
          npm install eslint --save-dev
          
      # Step 5: Execute Code Quality Analysis Umbrella
      - name: Run MegaLinter
        uses: oxsecurity/megalinter@v8
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: true
          ENABLE_LINTERS: JAVASCRIPT_ES
          APPLY_FIXES: none
